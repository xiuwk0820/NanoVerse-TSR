# Checkpoints

Do not commit `.pt` / `.pth` files.

Paper weights are **coming soon**. When they are hosted, save them here:

```
data/weights/
├── detector/
│   ├── cctsdb_yolo11s_world8_best.pt
│   ├── cctsdb_yolo11s_p2_best.pt
│   └── yolo_detector_best.pt
└── classifier/
    └── enhance_token_91.6_best.pth
```

Full links and init-weight URLs: [docs/WEIGHTS.md](../../docs/WEIGHTS.md)
